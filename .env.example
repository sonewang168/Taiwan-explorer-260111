# ==========================================
# 台灣探險圖鑑 v2 - 環境變數設定
# ==========================================

# === Render 部署網址（部署後取得）===
WEB_URL=https://your-app.onrender.com

# === Firebase 設定 ===
# 從 Firebase Console > 專案設定 > 服務帳戶 > 產生新的私密金鑰
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# === LINE Bot 設定 ===
# 從 LINE Developers Console 取得
LINE_CHANNEL_ACCESS_TOKEN=your_channel_access_token
LINE_CHANNEL_SECRET=your_channel_secret

# === Google API 設定（相簿 + 文件整合）===
# 從 Google Cloud Console 取得
# 1. 建立專案 > API 和服務 > 憑證 > 建立 OAuth 用戶端 ID
# 2. 應用程式類型選「網頁應用程式」
# 3. 授權的重新導向 URI 填：https://your-app.onrender.com/auth/google/callback
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=https://your-app.onrender.com/auth/google/callback

# === 其他 ===
NODE_ENV=production
PORT=3000
